Ты — senior Python разработчик. Создай проект Telegram-бота, который
интегрируется с Microsoft Exchange через EWS и отправляет уведомления о встречах
и почте. Соблюдай все пункты ниже, включая нюансы и форматы сообщений.

1) Общая архитектура
- Python-проект с модулем запуска `python -m notifier`.
- Хранение настроек: сначала брать значения из переменных среды, если они не
  заданы — из `.env`. Использовать `load_dotenv(override=False)`.
- Все даты/время внутри системы хранить в UTC. Отображение — в локальном
  часовом поясе `LOCAL_TIMEZONE`.
- Данные кэшируются в памяти: список встреч на сегодня и список непрочитанных
  писем.
- Два Telegram-бота:
  - APPOINTMENT_BOT — уведомления о встречах, обработка команд.
  - MAIL_BOT — уведомления о письмах.
- Ограничение по чатам: работать только с `ALLOWED_CHAT_IDS`.

2) Настройки (env/.env)
Обязательные:
- EWS_SERVER, EWS_EMAIL, EWS_USERNAME, EWS_PASSWORD
- UPDATE_INTERVAL, APPOINTMENT_REFRESH_INTERVAL, APPOINTMENT_NOTIFY_INTERVAL,
  MAIL_REFRESH_INTERVAL
- APPOINTMENT_BOT_TOKEN, MAIL_BOT_TOKEN
- ALLOWED_CHAT_IDS, ADMIN_CHAT_ID
- LOCAL_TIMEZONE

Опциональные:
- EWS_AUTH_TYPE (NTLM/BASIC/DIGEST), по умолчанию NTLM
- EWS_VERIFY_SSL (true/false), по умолчанию true
- KEYWORDS (через запятую) — ключевые слова
- MENTION_TEXT — текст упоминания (по умолчанию @poznik)
- AGENDA_TIME — время ежедневной сводки (HH:MM, локальное)
- LOG_LEVEL

3) Подключение к Exchange (EWS)
- Использовать exchangelib, autodiscover = false.
- Загружать встречи через calendar.view(start, end) на границах текущего дня
  в локальном времени (00:00 — 00:00), переводить в UTC.
- Загружать непрочитанные письма из inbox (is_read=False).
- Извлекать поля:
  - Встречи: subject, start, end, location, id, organizer.
  - Письма: subject, datetime_sent, sender, text_body/body, id.
- Время из EWS переводить в стандартный UTC datetime через timestamp.

4) Кэш и обновления
- Каждые UPDATE_INTERVAL секунд получать снапшот (встречи на сегодня + непрочитанные).
- Кэш всегда соответствует актуальным данным Exchange (полная замена).
- Для встреч хранить флаг "оповещена". Если встреча уже оповещена, но ее
  время сдвинулось вперед — снять флаг, чтобы оповестить повторно.
- Для удаленных встреч удалять метки оповещений.
- Для писем оповещение только один раз для каждого id.
- Циклы оповещений по встречам/почте запускать только после первого успешного
  обновления данных из Exchange.

5) Уведомления о встречах
- Каждые APPOINTMENT_REFRESH_INTERVAL секунд проверять встречи, которые
  начинаются в пределах APPOINTMENT_NOTIFY_INTERVAL секунд от текущего времени.
- Уведомлять один раз на каждую встречу (кроме случая, когда встреча сдвинулась
  вперед — тогда уведомить повторно).
- Если несколько встреч стартуют одновременно — отдельные сообщения на каждую.
- Формат сообщения (MarkdownV2):
  *🔔 Через X мин: Y*
  Организатор: *NAME*
  Начало: DD.MM.YYYY HH:MM
  Длительность: N минут
  [доп. строка]
- Если location содержит URL (http/https) — строка "Ссылка: URL" и inline-кнопка
  "подключиться" с этим URL.
- Если location есть, но URL нет — строка "Место: X".
- Если location пустой — строка не добавляется.

6) Уведомления о почте
- Каждые MAIL_REFRESH_INTERVAL секунд отправлять уведомления о новых
  непрочитанных письмах (по id), каждое письмо — отдельное сообщение.
- Формат (MarkdownV2):
  *subject*
  Отправитель: NAME
  Дата отправки: DD.MM.YYYY HH:MM

  > первые 2 непустые строки письма (не более 200 символов), как цитата
- В цитате показывать только видимый текст: удалять HTML/разметку, CID-вставки,
  ссылки и мусорные строки.
- Если совпали KEYWORDS (по subject/sender/preview), добавить:
  - В начало сообщения префикс "‼️"
  - В конец — строку с MENTION_TEXT (если задана)

7) Команды
- /today: доступна только в ALLOWED_CHAT_IDS, ответ в MarkdownV2.
  Формат:
  *Сегодня DD.MM.YYYY*
  - каждая встреча: "‣Subject, HH:MM, H:MM"
  - окна между встречами: "> *Окно*: начало HH:MM, длительность H:MM"
  - рабочий день начинается в 09:00 (локально). Если первая встреча позже 09:00,
    добавить окно от 09:00 до начала первой встречи.
  - если встреч нет: "Сегодня встреч нет"
- /check: список пересечений (параллельных встреч), MarkdownV2.
  Формат:
  Всего пересечений: N
  *Пересечение i:* XX минут
  Название, HH:MM, H:MM
  Название, HH:MM, H:MM
  (между блоками пустая строка)
  Длительность пересечения — суммарное время, когда одновременно активны 2+ встреч.

8) Ежедневная сводка (AGENDA)
- Если AGENDA_TIME задано, в рабочие дни (Пн–Пт) в это время отправлять
  два сообщения: результат /today и /check.
- Если отправка не удалась — повторять попытки каждые 60 секунд, максимум
  10 попыток, затем прекращать на этот день.
- Запускать AGENDA только после первого успешного обновления данных из Exchange.

9) Логи и ошибки
- Логировать:
  - старт обновления Exchange;
  - завершение обновления (кол-во будущих встреч, минуты до ближайшей,
    кол-во новых непрочитанных писем);
  - старт циклов оповещений (сколько сущностей, когда следующий запуск).
- Ошибки Exchange писать только в логи.
- При ошибке авторизации Exchange остановить попытки обновления (update loop).
- Заглушить подробные логи httpx/httpcore (уровень WARNING).

10) Docker
- Dockerfile на базе python:3.12-slim.
- docker-compose.yml с переменными в формате KEY: ${KEY}, чтобы приоритет был
  у переменных среды, а .env использовался как fallback.
- .dockerignore для исключения .env, .venv, __pycache__ и т.п.

11) Доп. требования реализации
- Использовать python-telegram-bot >= 20, exchangelib >= 5, python-dotenv.
- Экранить MarkdownV2 корректно.
- Все отправки сообщений в Telegram выполнять асинхронно.
- Никаких внешних БД — только in-memory кэш.
